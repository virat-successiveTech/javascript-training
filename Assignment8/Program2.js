// Find Error in below questions and rectify them

let myObject = { name: 'John', age: 30 };
for (property in myObject) 
    {
   console.log(property + ': ' + myObject[property]);//changed myObject.property to 
   // myObject[property] for geeting the values from the object 
}

